using Gtk 4.0;
using Adw 1;

template $KeyboardRow: Adw.ActionRow {
  [prefix]
  CheckButton select_button {}

  activatable-widget: select_button;

  [suffix]
  Label suffix_bin {
    justify: center;
    label: _("key");

    styles [
      "monospace",
      "dim-label",
    ]
  }
}
