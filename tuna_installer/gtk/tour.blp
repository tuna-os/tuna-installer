using Gtk 4.0;
using Adw 1;

template $VanillaTour: Adw.Bin {
  halign: fill;
  valign: fill;
  hexpand: true;

  Adw.StatusPage status_page {
    halign: fill;
    valign: fill;
    hexpand: true;

    Picture assets_svg {
      can-shrink: false;
    }
  }
}
